print "test"
let pub eeeeee:int = 0
let e = [\{a}, \n]
print eeeeee

type pub state = <alive float>
  | pub <dead int list[str]>
  | pub unborn

alias pub pair[t] = (t, t)
alias pub strPair = pair[str]

let pub secret = <alive 3.4>
