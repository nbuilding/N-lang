class pub Test {
	let test:int = 0

	New = [] {
		var test = 2
	}

	let thingo = [a:int b:int] -> int{
		return a + b + test
	}
}

let test = [a:int b:int] -> int {
	return a + b
}


print(test(1, 2)
              |> test(1))

let tester:Test = Test.new()

print(tester.thingo(1, 2))