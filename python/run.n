import fek
import SystemIO
import json
import request

let test = [a:int b:int] -> int {
	return a + b
}


print (test(1, 2)
	           |> test(1))

class test {
	let test:int = 0

	let thingo = [a:int b:int] -> int{
		return a + b + test
	}
}