import request

// important variables
let key:str = ""

// custom types

type status = online
            | away
            | offline

alias role = {
	color:color
	name:str
	rank:int
}

alias member = {
	id:str
	name:str
	nickname:str
	icon:str
	roles:list[role]
	bot:bool
	status:status
	customStatus:maybe[str]
}

alias guild = {
	id:str
	name:str
	emojis:list[str]
	icon:str // link maybe idk
	members:list[member]
	banlist:list[member]
}

alias channel = {
	id:str
	name:str
	guild:guild
	membersAllowed:list[member]
}

alias message = {
	id:str
	content:str
	sender:member
	textChannel:bool
}

alias reaction = {
	emoji:str
	message:message
	sender:member
}

alias uauauau = {
	id:str
}

// functions
let pub start = [k:str] -> () {
	var key = k
	// other important functions idk what is going to happen here
}

let pub getKey = [] -> str{
	return key
}
