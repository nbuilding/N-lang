// Based on docs/features/async.md

import times

let pub main = () |> [] -> cmd[()] {
	times.sleep(101)!
	for (item in [100, 200, 300]) {
		if item == 200 {
			print("Hello")
		} else {
			times.sleep(item)!
		}
	}
	times.sleep(102)!
}
